export const appConfig = {
  name: process.env.NEXT_PUBLIC_APP_NAME || '小红书运营',
  shortName: process.env.NEXT_PUBLIC_APP_SHORT_NAME || 'XHS',
  subtitle: process.env.NEXT_PUBLIC_APP_SUBTITLE || '通用运营后台',
}
